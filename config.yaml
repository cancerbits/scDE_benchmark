# project name and name of docker image
project_name: scDE_benchmark
project_docker: cancerbits/scDE_benchmark

# project-specific paths 
# only the ones ending in _host should be changed
# the others are paths used in the docker container
project_root_host: /home/christoph_h/bioinf_isilon/Research/HALBRITTER/Internal/users/christoph_h/projects/scDE_benchmark
out_root_host: /home_christoph_h/bioinf_isilon/Research/HALBRITTER/Internal/out/scDE_benchmark

project_root: /home/rstudio
out_root: /home/rstudio/mnt_out
